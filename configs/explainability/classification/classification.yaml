# Classification Explainability
method: "integrated_gradients"  # integrated_gradients | grad_cam
integrated_gradients:
  steps: 50
  internal_batch_size: 1
grad_cam:
  layer_name: null  # Auto-detect last conv layer if null
